/**
 * Reports dashboard configuration.
 *
 * Deploy the Apps Script web app (see apps-script/DEPLOY-reports.md) and paste
 * its /exec URL below to fetch live data from the private Google Sheet.
 * Leave it empty ("") to run in demo mode using the bundled REPORTS_SEED data.
 */
const REPORTS_SCRIPT_URL = "";
